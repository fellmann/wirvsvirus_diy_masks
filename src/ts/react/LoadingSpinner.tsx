import * as React from 'react'

const LoadingSpinner = () => (
  <div style={{ display: 'flex', justifyContent: 'center' }}>
    <div className="cls-spinner">
      <div className="cls-circle cls-spin"/>
    </div>
  </div>
)

export default LoadingSpinner
